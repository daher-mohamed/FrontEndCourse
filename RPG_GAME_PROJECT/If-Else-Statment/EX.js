//play Health check
const playerHealth = 40;
console.log(playerHealth > 10 ? "Player is Strong " : "player is weak");

//Player Gold Check
const playerGold = 70;
console.log(playerGold => 1 && playerGold <= 100 ? "Rich player" : "player poor");

//Nested if-Else
const weather = "sunny", timeDay = "morning";
console.log(weather === "sunny" && timeDay === "morning" ? "continue the Adventure" : timeDay === "morning" ? "continue the Adventure" : "Seek shelter");

//Comparing Weapon Strength
const playerWeapon = "M16", enemyWeapon = "M16";
console.log(playerWeapon === enemyWeapons ? "Weapons clashing" : "Advantage");

//Check Pkayer Age
const playerAge = 39;
console.log(playerAge >= 13 && playerAge <= 19 ? true : false);

//Game Score
const gameScore = 40;
if(gameScore >= 50){
    console.log(true);
}else{
    console.log(false);
}

//Number of Enemies
const numOfEnemies = 10;
console.log(numOfEnemies % 2 === 0 ? "Even number of enemies" : "Odd Number of enemies");

//character Age
const characterAge = 17;
console.log(characterAge < 12 ? "Youngling" : characterAge >= 13 && characterAge <= 19 ? "Adolescent" : "adult");

//Gmae Session
const sessionDuration = 30;
if(sessionDuration < 90) {console.log("short");
}
else if(sessionDuration >= 90 && sessionDuration <= 150){console.log("Moderate");
}
else{
    console.log("Marathon");
    
}

//Potion Avaliability
const totalPotions = 70, usedPotions = 30;
console.log(totalPotions - usedPotions > 0 ? "Postions available" : "Out of potions");

//validate Player Age
const playerage = 23, questAgeRestruction = 18;
console.log(playerage >= questAgeRestruction ? "you can play" : "soory you can't play :(");

//special Weekend Events
const dayOfWeek = "Sunday"
console.log(dayOfWeek === "Sunday" || dayOfWeek === "Saturday" ? "Special event happening" : "Regular gameplay");

//Identify Era
const gameSettingYear = 1200;
console.log(gameSettingYear >= 900 && gameSettingYear <= 1500 ? "Medieval era setting" : "Different historical are");

//Evaluate Character
const characterClass = "Warrior", characterLevel = 11;
console.log(characterClass === "Warrior" && characterLevel >= 10 ? "Powerful warrior" : "Average character");

//Determine Battle
const playerhealth = 40, enemyCount = 50;
console.log(playerhealth > 50 && enemyCount < 3 ? "Ready for battle" : "Proceed with caution");

//Assess Quest
const questProgress = 17, qoustDifficulty = 17;
console.log(questProgress === 100 && qoustDifficulty <= 5 ? "Quest completed" : "Quest in progress");

//check Player
const inventorySize = 15;
console.log(inventorySize >= 90 ? "Inventory almost full" : "Planty of space in inventory");




 







